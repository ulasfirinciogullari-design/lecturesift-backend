name: Apply Resend failure classification

on:
  push:
    branches:
      - fix/classify-resend-403
    paths:
      - .github/workflows/apply-resend-classification.yml

permissions:
  contents: write

jobs:
  apply-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Check out classification branch
        uses: actions/checkout@v4
        with:
          ref: fix/classify-resend-403
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Apply classification patch
        run: python scripts/apply_resend_classification.py

      - name: Install system dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq ffmpeg

      - name: Install Python dependencies
        run: python -m pip install -r requirements.txt pytest

      - name: Run backend tests
        run: python -m pytest -q

      - name: Validate frontend JavaScript
        run: |
          node --check frontend/app.js
          node --check frontend/email-auth.js

      - name: Commit validated classification
        run: |
          rm -f scripts/apply_resend_classification.py .github/workflows/apply-resend-classification.yml
          git config user.name "LectureSift CI"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git diff --cached --quiet && exit 0
          git commit -m "feat: classify Resend delivery failures safely"
          git push origin HEAD:fix/classify-resend-403
