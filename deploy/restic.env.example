# Copy to /etc/lecturesift/restic.env on the VPS and chmod 600.
# This file is sourced only by root-owned backup/recovery scripts. It must
# never be passed to the public API or worker containers.

RESTIC_REPOSITORY=
RESTIC_PASSWORD=
RESTIC_AWS_ACCESS_KEY_ID=
RESTIC_AWS_SECRET_ACCESS_KEY=
